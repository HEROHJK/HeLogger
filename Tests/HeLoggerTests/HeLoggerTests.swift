    import XCTest
    @testable import HeLogger

    final class HeLoggerTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(HeLogger().text, "Hello, World!")
        }
    }
